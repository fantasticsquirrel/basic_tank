#pragma once

// Copy to secrets.h, then replace all three values. secrets.h is git-ignored.
namespace secrets {
constexpr char WIFI_SSID[] = "YOUR_WIFI_NAME";
constexpr char WIFI_PASSWORD[] = "YOUR_WIFI_PASSWORD";
constexpr char CONTROL_TOKEN[] = "replace-with-a-long-random-control-token";
}  // namespace secrets

